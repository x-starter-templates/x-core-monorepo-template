/** @typedef  {import("prettier").Config} PrettierConfig */

/** @type { PrettierConfig } */
const config = {
	tabWidth: 2,
	useTabs: true,
	semi: true,
	printWidth: 80,
	singleQuote: true,
	trailingComma: 'all',
	arrowParens: 'always',
};

export default config;
